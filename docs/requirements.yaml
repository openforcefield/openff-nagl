name: openff-nagl-docs
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

  - myst-nb
  - myst-parser>=0.14
  - docutils
  - sphinx

    # Pip-only installs
  - pip:
    - git+https://github.com/openforcefield/openff-sphinx-theme.git@main
