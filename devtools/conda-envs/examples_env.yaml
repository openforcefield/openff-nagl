name: openff-nagl-test
channels:
  - dglteam
  - openeye
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # Examples
  - jupyterlab
  - ipywidgets<8
  - nglview

  # UI
  - click
  - click-option-group
  - tqdm
  - rich

  # chemistry
  - openff-toolkit >=0.11.1
  - openff-units
  - pydantic <3
  - rdkit
  - openeye-toolkits

  # database
  - pyarrow

  # gcn
  - dglteam::dgl >=1.0
  - pytorch
  - pytorch-lightning
  - tensorboard

  # parallelism
  - dask-jobqueue

