name: openff-nagl-test
channels:
  - openeye
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

  # UI
  - click
  - click-option-group
  - tqdm

  # chemistry
  - openff-recharge
  - openff-toolkit-base >=0.11.1
  - openff-units
  - pydantic <3
  - rdkit
  - scipy
  - ambertools

  # # database
  # - pyarrow

  # gcn
  - pytorch >=2.0
  - pytorch-lightning

  # parallelism
  - dask-jobqueue

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
