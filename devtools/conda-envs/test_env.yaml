name: gnn-charge-models-test
channels:
  - conda-forge
  - defaults
dependencies:
    # Base depends
  - python
  - pip

  - click
  - tqdm
  - openff-toolkit
  - openff-units

    # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

